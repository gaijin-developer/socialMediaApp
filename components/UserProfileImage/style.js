import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  userImageContainer: {
    borderColor: '#f35bac',
    borderWidth: 1,
    padding: 3,
    borderRadius: 70,
  },
  image: {
    width: 65,
    height: 65,
  },
});
export default style;
